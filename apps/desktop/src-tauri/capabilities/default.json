{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LF-001 の最小 capability。既定の core 権限と、この shell が公開する読取 command だけを許可する。plugin の追加は用途ごとに別 capability で行う。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
