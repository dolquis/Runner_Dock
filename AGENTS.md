# 開発エージェント向け指示

対象はRunner Dock。WindowsネイティブとWSL2 UbuntuのGitHub Actions Runnerを統合管理する、新規独自実装です。

## 着手時

`README.md`、`START_DEVELOPMENT.md`、`docs/02_REQUIREMENTS.md`、`docs/03_ARCHITECTURE.md`、`docs/09_SECURITY.md`、`docs/17_ADR.md`を読みます。割り当てられたLFタスクの依存関係を`docs/14_BACKLOG.md`で確認します。

文書パッケージの段階ではソースコード、Cargo workspace、Node scriptsはまだ存在しません。将来の構成図を既存実装として扱わないでください。

## 絶対に守る設計境界

1. HomeRunをフォークしません。ソースやUI資産を無断で移植せず、参照元・参照した知見を記録します。
2. UIはRunner、OSシェル、GitHubトークン、DBを直接操作しません。操作は型付きの許可リスト経由です。
3. 日常運用のAgent・Runnerを管理者またはLinux rootで動かしません。昇格は限定的な初回設定処理に分離します。
4. GUI終了とNode停止を同一視しません。反対にAgentクラッシュ後に子プロセスが安全に残るとも仮定しません。
5. 既存サービス、ディストリビューション、Workflow、秘密情報は、明示的に取り込んだ対象以外変更しません。
6. `wsl --shutdown`、`wsl --unregister`、サービスのワイルドカード停止、曖昧なRunner名への「最初の1件」フォールバックを通常操作に入れません。
7. GitHub APIエラーや期限切れを`Offline`、`Idle`、成功に丸めません。`Unknown`と鮮度を保持します。
8. ローカル優先ルーティングは事前判定です。キュー待ちや実行中ジョブの無停止移行を実装済みと表現しません。
9. `busy == false`は真偽値の完全一致で判定します。`jq '.busy // true'`は使いません。
10. 公開PR、同一repo内PRを含む未信頼コードは、既定でself-hostedへ送らない設計です。ただし生成Workflowだけをセキュリティ境界と考えてはいけません。

## 実装ルール

- Rust stableを使い、`rust-toolchain.toml`と`Cargo.lock`を固定します。nightlyやRCは承認済みADRがある場合だけです。
- `core`はTauri、WSLコマンド、GitHub HTTPクライアントに依存させません。実行・時計・秘密情報・保存先はインターフェースで注入します。
- CPU/ブロッキングI/OをGUIやTokio executor上で長時間占有させません。キャンセル・タイムアウト・上限付きバッファを用意します。
- 外部文字列を`cmd /c`、`bash -c`へ連結しません。固定ラッパーや引数配列を使い、Windowsの引数処理もテストします。
- `unsafe`はOS連携に閉じ込め、必要理由、不変条件、対象APIの資料、テストを付けます。
- Token、Authorizationヘッダー、登録応答、Runner資格情報をログや診断ZIPへ含めません。
- Reactはイベント購読の解除、不要な再取得の防止、ログの仮想化、型付きエラー状態を実装します。レンダー中に副作用を起こしません。
- IPC、DB migration、生成型、ロックファイルは競合しやすい共有境界です。同時編集は統合担当の了承を得ます。

## テストと報告

モックで動いたこととWindows/WSLで動いたことを分けます。実行していないテストは「未実行」と明記し、成功と記載しません。実機でしか確認できない項目を無断でskipして完了扱いにしません。

各PRには、LFタスクID、変更範囲、対応FR、実行コマンドと結果、未確認事項、リスク、ロールバック方法を含めます。失敗の再現テストを先に追加する方針を基本とします。

## エージェントの権限

文書やソースの変更依頼は、ユーザーのGitHub App作成、PAT発行、Secrets変更、課金設定変更、Runner登録、サービス停止、公開リリースまでを許可するものではありません。外部アカウントやPC設定への変更はタスクの明示的な許可範囲でのみ行います。

CIの自己管理機能を開発中のAgent自身に無条件で適用しないでください。自分を停止・更新するテストは隔離されたテスト用環境で実施します。
